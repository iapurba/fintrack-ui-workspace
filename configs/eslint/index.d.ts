import type { Linter } from "eslint";

/**
 * Declares the shape of the default export from @fintrack/eslint-config.
 * It's an array of ESLint Config objects.
 */
declare const config: Linter.Config[];
export default config;
